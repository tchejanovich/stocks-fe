export const tickerSelector = (state) => state.ticker;
export const fromDateSelector = (state) => state.fromDate;
export const toDateSelector = (state) => state.toDate;
export const resultsSelector = (state) => state.results;
